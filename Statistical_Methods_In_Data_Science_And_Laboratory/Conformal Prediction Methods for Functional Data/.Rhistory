## ============================================================
## GLOBAL SEED (set once at top of script for reproducibility)
## ============================================================
set.seed(2026)
## --- Dataset creation parameters ---
zip_path       <- "data.zip"
data_root      <- "data"
trim_sec       <- 5       # Omit first & last seconds (handling artifacts)
window_len_sec <- 10      # Seconds per curve (realism + sample size balance)
step_sec       <- 10      # Non-overlapping windows (exchangeability)
M_grid         <- 200     # Grid points per curve (effective 20 Hz for 10s)
